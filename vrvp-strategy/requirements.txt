# VRVP Strategy - Requirements
#
# RECOMMENDED: Use install.sh for proper installation:
#   ./install.sh              # Install in current environment
#   ./install.sh --venv       # Create and use virtual environment
#
# Manual installation (if needed):
#   pip install smartmoneyconcepts==0.0.26 --no-deps
#   pip install -r requirements-main.txt

# Core dependencies (install via requirements-main.txt)
-r requirements-main.txt

# smartmoneyconcepts requires --no-deps due to outdated dependency requirements
# Install manually: pip install smartmoneyconcepts==0.0.26 --no-deps
smartmoneyconcepts==0.0.26