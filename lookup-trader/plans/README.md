# Plans

Design and implementation plans for upcoming features. These are working documents — not committed specs until implemented.

| Plan | Status | Summary |
|------|--------|---------|
| [bar-pattern-tagging.md](./bar-pattern-tagging.md) | Draft | Precompute pattern/setup tags on every bar, persist in `bar_features`, integrate with `ComparePanel` and compare/base-rate services |

**Next up:** Phase 1 of bar-pattern-tagging — rule-based candlestick taggers + `build_bar_features.py` + tag display in ComparePanel.
